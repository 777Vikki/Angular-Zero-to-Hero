import { HighlightHostbinding } from './highlight-hostbinding';

describe('HighlightHostbinding', () => {
  it('should create an instance', () => {
    const directive = new HighlightHostbinding();
    expect(directive).toBeTruthy();
  });
});
