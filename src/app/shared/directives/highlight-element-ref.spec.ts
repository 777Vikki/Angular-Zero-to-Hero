import { HighlightElementRef } from './highlight-element-ref';

describe('HighlightElementRef', () => {
  it('should create an instance', () => {
    const directive = new HighlightElementRef();
    expect(directive).toBeTruthy();
  });
});
